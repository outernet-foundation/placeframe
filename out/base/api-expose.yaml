apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: placeframe
  labels:
    com.docker.compose.project: placeframe
    com.docker.compose.service: api
spec:
  selector:
    com.docker.compose.project: placeframe
    com.docker.compose.service: api
  ports:
  - name: api-8000
    port: 8000
    targetPort: api-8000
