# Polyrepo initiative — thread index

Five threads. Execution order (resequenced from the original 1-2-3-4; the flip of the last two and the deferral of the stable cutover are rulings recorded in the Session resume). Thread 4 is parked behind thread 5 (deduplication maximalism ruling, 2026-09-21):

1. **prepo** — [polyrepo-workbench.md](polyrepo-workbench.md). Expand pgit into the polyrepo workbench: workspace transparency (discovery-based enumeration, the VSCode extension, pane naming), the sync verbs, and `sync` dependency provisioning (`uv sync` + Unity batch no-op refresh across the discovery set). The local dependency overlay (git filter pair, `link`/`unlink`) was built and then deleted — undercooked, its complexity undesirable at this time (operator ruling 2026-09-20, prepo `0e04450`); registry pins are the whole consumption story, in-repo and cross-repo. Complete.
2. **Publishing** — [package-publishing-redesign.md](package-publishing-redesign.md). In-house UPM derivation, nuget + npm + PyPI feeds, the publication-machinery repo, the immutable dev channel. Items 1-10 complete — the dev channel is live and proven. Items 11-13 (stable cutover, UnityNuGet curated-list removal, deprecations) are deferred behind threads 3 and 4: `main` stays untouched, external consumers see only `-dev` prereleases for the window, and the dev channel stays dispatch-triggered until the eventual release merge lands the workflow on `main`.
3. **Internal tools on registries** — [internal-tool-registries.md](internal-tool-registries.md). Convert every remaining intra-org package reference from git URL to registry pin by publishing the tool tier (bashrun, logconf, stack-lifecycle, unity-buildkit, openapi-clientgen, pubpkg) to PyPI — four of the six renaming on the way (logconf → `logger-conf` — its PyPI name is foreign-owned and the published placeframe-common wheel resolved `logconf[otlp]` onto the foreign package; stack-lifecycle → `stack-toolkit`; openapi-clientgen → `openapi-client-codegen`; pubpkg → `release-kit` — PyPI's similarity registrar rejects near-existing names). Runs before the extraction so extracted repos are born registry-pinned and the git-pin lockstep cascade (every unity-buildkit change forcing pubpkg + placeframe bumps) dies first. The CaptureTool manifest repoint to npmjs exact pins — the Unity-side registry-consumption proof — was this thread's closing bridge step into thread 4. **Complete.**
4. **Repo boundaries** — [repo-extraction.md](repo-extraction.md). Extract the capture tier into a single standalone repo (`placeframe-capture-tool`; one repo, not two: the AOA tether couples the phone app to the box stack, and same-repo consumption keeps `PlaceframeZedCaptureClient`'s only in-repo consumer co-located); the cross-repo contracts; fresh-clone autonomy. A first bootstrap attempt (2026-09-21) was aborted on a **no-duplicated-first-party-logic** ruling — the scaffolding had copied `lock_python`, the codegen orchestrator, and preflight internals instead of consuming them — and the thread is **parked behind thread 5**; its change-set item 1 is now a repin of the consolidated tooling.
5. **Tooling consolidation** — [tooling-consolidation.md](tooling-consolidation.md). The org-wide deduplication program born from thread 4's audit: the `-devkit` family (python-devkit new; unity-buildkit/stack-toolkit/release-kit renamed to unity-devkit/docker-devkit/release-devkit while the rename window is cheap), stock mirror consumption for loki/alloy (the `observability-images` repo was cancelled before creation — probe ruling 2026-09-22, plan in [observability-stock-consumption.md](observability-stock-consumption.md)), the codegen orchestrator verb in openapi-client-codegen, shared workflows (reusable for checks, composite actions for publishes — grounded in the PyPI/npm trusted-publisher research), and the ruff canonical + drift gate. logger-conf, bashrun, openapi-client-codegen's name, and release-kit's wholeness all explicitly preserved.

Each doc is self-contained: problem, ground truth, design, change set, discarded alternatives, open parameters. Cross-thread couplings are recorded as facts inside the docs where they bite (e.g. boundaries' cross-repo consumption requires the feeds to exist; publishing's stable cutover is deferred behind threads 3 and 4).

## Pre-merge review-tractability pass

Before the dev merge, `polyrepo-initiative`'s commits (58 as of this plan) folded into 9: seven code commits plus two prose capstones. **Landed** — the fold merged to `dev`, and `dev` is pushed and in sync with origin. The design docs are the initiative's record — rulings, discarded alternatives, and landed-with notes live in the doc bodies at higher fidelity than commit messages — so git history must not duplicate that record; the bulk of the branch is pure checkpoint/bookkeeping noise. The fold rode a **new** branch (`package-publishing-redesign`, named for the [plan doc](package-publishing-redesign.md) since the branch's changes are the publishing thread), never by rewriting `polyrepo-initiative`: the PR rides the fold branch and `origin/polyrepo-initiative` simply goes stale — no force-push anywhere.

Method — path-scoped rebuild from the original trees, not a cherry-pick replay (deferring prose across picks leaves `.md` files dirty and breaks subsequent picks; snapshot restores cannot conflict). Prose placement rule: every `.md` change lands in a prose capstone unless it is a carve-out — a doc change belonging to a code change's own review story rides with that code commit:

1. Backup ref first (`git branch backup/polyrepo-initiative-pre-review-fold` — the repo's `backup/*` convention). Work in a `prepo new` worktree; the primary checkout carries pre-existing dirt that must not enter commits or stashes.
2. `git checkout -b package-publishing-redesign origin/dev`. The merge-base is `origin/dev`'s tip (`d7e85958`); `origin/polyrepo-initiative`'s one commit beyond dev (`04a6aba4`) is simply the first fold input.
3. For each code group below, `git restore -s <boundary> --staged --worktree -- . ':(exclude)*.md'` from the group's boundary commit — its chronologically-last code commit (`0a870fad`, `94404027`, `c0a0730d`, `f66c158c`, `84532348`, `99e0e29e`, `6e92c6f8`) — then commit. Restore converges matched paths to the source state, deletions included; no commit after `6e92c6f8` touches a non-`.md` path, so code-at-boundaries + prose-at-tip partitions the tip tree exactly.
4. Carve-outs into the rename commit (group 2): the two package READMEs restored from `94404027` (`ffbb6fa2`/`544f4240` carry the rename's doc mentions) and `f78cf65c`'s six agent-doc updates restored from `f78cf65c`. A carve-out is safe only when no later commit in the range touches the file — verified by a last-touch audit over all eight; a file touched again later must ride to a capstone instead.
5. Two prose capstones land last: `git restore -s <tip> -- '*.md'`, commit the non-`design` files as the agent-docs capstone, then `design/` — carrying this plan's own rewrite — as the final design-docs capstone.
6. Verify: the tree diff against `polyrepo-initiative` is **empty** before this doc's rewrite (the rewrite is the review branch's only delta — no preflight re-run needed); the code/prose classifier over `origin/dev..polyrepo-initiative` partitions into exactly the 14 code commits named in groups 1-7 plus prose commits only — every sha mapped to a group; and every commit outside the two capstones touches non-`.md` paths only, the rename commit excepted for its eight carved files.

The nine commits, chronological, target subjects exact:

| # | Subject | Folds |
|---|---|---|
| 1 | Absorb the publish cohort into pubpkg | bb7dd462, 0a870fad |
| 2 | Rename the Python packages to placeframe-common and placeframe-core | ffbb6fa2, 9123750a, 544f4240, 94404027 + carve-outs: the two package READMEs, `f78cf65c`'s agent docs |
| 3 | Finalize the Unity and PyPI publish set | c0a0730d |
| 4 | Wire the npm identity into codegen and repoint consumers | 6037fce9, 014002f7, f66c158c |
| 5 | Run generate-clients | 84532348 — the canonical codegen commit; exact message, standalone |
| 6 | Add Unity meta files and regenerate package locks | b854b548, 99e0e29e |
| 7 | Publish dev prereleases from the release workflow on green dev pushes | 6e92c6f8 |
| 8 | Update agent docs and README for the publishing thread | every remaining non-`design` prose change |
| 9 | Add the polyrepo initiative design docs | every `design/` prose change — the capstone, landed last |

## Session resume

Thread 1 (prepo) is complete; its overlay half was subsequently deleted whole — undercooked, undesirable complexity at this time (operator ruling 2026-09-20; see the thread list) — leaving workspace transparency, the sync verbs, and `sync` dependency provisioning as prepo's surface. Thread 2 (publishing) is complete through item 10 (items 11-13 — stable cutover, UnityNuGet curated-list removal, deprecations — deferred behind threads 3 and 4; `main` stays untouched and every dev publish stays hand-dispatched until the release merge lands the workflow on `main`). **Thread 3 (internal tools on registries) is complete**: all six tool packages live on PyPI — logger-conf, bashrun, stack-toolkit, unity-buildkit, openapi-client-codegen at 0.1.1, release-kit at 0.1.0 — every intra-org git pin is converted (placeframe's `[tool.uv.sources]` carries only third-party `lightglue`), release-kit is uvx-invoked everywhere, and the CaptureTool bridge repoint built an APK against npmjs exact dev pins. See [internal-tool-registries.md](internal-tool-registries.md) for the landed record.

Thread 4 (repo boundaries) remains **fully specified and audited** but is **parked** behind thread 5 (operator ruling 2026-09-21). Its history: every boundary parameter decided 2026-09-21 (one repo `placeframe-capture-tool`, repo-scoped GHCR namespace; paired phone↔box compat; contract homes; one release unit on the standard release flow; fresh start + seeded `capture-tool-v0.1.2` ledger; dispositions) — then a first bootstrap attempt was aborted at the operator's direction after its scaffolding duplicated first-party logic (`lock_python.py` verbatim, a slimmed codegen orchestrator, a slimmed preflight; zero commits landed, the scaffold deleted, placeframe untouched). The audit and the file-level reconnaissance live on in [repo-extraction.md](repo-extraction.md) (duplication-audit table + Reconnaissance ground truth) and remain load-bearing for the eventual un-parking at thread 5's Phase 6.

**Thread 5 (tooling consolidation) is the active thread** — [tooling-consolidation.md](tooling-consolidation.md) carries the full program. **Phase 0/1 complete; Phase 2 item 5 complete** (2026-09-21): the renames landed (docker-devkit/unity-devkit/release-devkit live on PyPI; old ci.yml publish jobs split into `release.yml` — the trusted-publisher filename), consumers repinned, and the `ci_step` extraction was corrected mid-flight into a fifth devkit — **`ci-devkit`, the CI runner floor** (`ci_step` + runner provisioning + `install_oras` + `git_tags` + the OCI artifact cache; release-devkit's ledger converged onto the tag primitives; unity-devkit's dependency on python-domain tooling dissolved). Live on PyPI: ci-devkit 0.1.0, unity-devkit 0.1.2, release-devkit 0.1.1, docker-devkit 0.1.1. **Phase 2 items 6 and 7 complete** (2026-09-21): `lock-python` lives in python-devkit — the neural-networks per-group export quirk generalized as the `[tool.python-devkit.lock-python]` `group-export-dirs` config table (glob → directory, deduplicated across members; the design-at-execution ruling and equivalence record live in items 6-7 of the [program doc](tooling-consolidation.md)) — and the preflight runner landed (`preflight_runner.run_checks` over `CommandCheck`/`GeneratedCheck`; placeframe's preflight thinned to checks + db setup + declarative lists, its floor imports rewired to `ci_devkit.*`). placeframe's copies deleted, its preflight calls both in-process, the lock-python entry point ships from the devkit. **Phase 2 item 8 complete** (2026-09-21): ruff canonicalization landed — the canonical ships as python-devkit package data, every repo (placeframe + all 8 mirroring repos) carries it as a verb-written `ruff.base.toml` under a hand-owned `ruff.toml` extend layer, `sync-ruff --check` drift-gates byte equality and layer shape, CI gates pin `uvx --from python-devkit==0.1.2` (red until the 0.1.2 release lands, leaves-first), `required-version = ">=0.14.11"` couples the ruff floor, and the full mechanism record — including the ruff-sync / native-extends architecture research verdict and the names-in-layers ruling — lives in item 8 of the [program doc](tooling-consolidation.md). The session's opening verification also landed: placeframe's lock regen added python-devkit 0.1.1 + ci-devkit 0.1.1 and floated unity-devkit to 0.1.3 (patch bumps beyond the resume's expectation, additive), with full preflight green. **Item 9 complete** (2026-09-22 session): python-devkit 0.1.2 verified via the Release run log (`Tagged: python-devkit-v0.1.2`, 2 files uploaded). Adoption-wave gates: four green on their pushed adoption commits — ci-devkit, release-devkit, unity-devkit, docker-devkit, whose adoption commits auto-released ci-devkit 0.1.2, release-devkit 0.1.2, unity-devkit 0.1.4, docker-devkit 0.1.3; three red — bashrun, openapi-client-codegen, logger-conf had synced their bases **before** `962995a` pinned the ruff floor into the canonical, leaving each exactly one line (`required-version`) stale; the gate caught it as designed, and the released verb re-synced all three (one-line commits, pending operator push for their gates to go green). placeframe's repin wave landed on `tooling-consolidation` (`89a478ca`): lock floated to python-devkit 0.1.2 / ci-devkit 0.1.2 / docker-devkit 0.1.3 / unity-devkit 0.1.4, the in-process `sync_ruff(check=True)` gate added beside `lock_python(check=True)` in preflight, pylock regen a no-op (the devkits are dev-group dependencies, never in per-service exports), full preflight green. **Phase 3 (item 10) complete** (2026-09-22): the `generate_projects` orchestrator verb landed in openapi-client-codegen (library API + `generate-projects` CLI over a **required injected `dump_spec` strategy** — design-corrected mid-flight at the operator's direction, restoring spec production as consumer-owned; six tests, full record in the [program doc](tooling-consolidation.md)), and placeframe's `generate-clients` thinned to config + verb call naming `dump_openapi_spec`, verified byte-identical over full `--no-cache` runs across both cuts. Pending: the operator push auto-releases openapi-client-codegen **0.1.2** (additive → patch-auto), then the next session floats `uv lock --upgrade-package openapi-client-codegen` and re-runs full preflight — until then placeframe's client-codegen gate is red (leaves-first, item-6 pattern). The `observability-images` repo + mount were never created — Phase 4 was replaced 2026-09-22 by [stock consumption](observability-stock-consumption.md) (staged plan awaiting implementation). Still outstanding from earlier threads: the publishing thread's deferred items 11-13 — the UnityNuGet curated-list removal and the `PlaceframeZedClient` deprecation ride the boundaries cutover window (thread 4's change-set item 8); the stable cutover follows after.

**RUF067 strict-init wave landed** (2026-09-22, operator ruling; item 17 of the [program doc](tooling-consolidation.md)): canonical `strictly-empty-init-modules`, four library inits emptied, consumers repointed to submodule paths across 13 repos, `__all__` outlawed (alias-form re-exports instead; mechanical ban needs a layout-lint verb — **proposed, not built; awaiting operator go**), `from . import module` enforcement deferred with only the named cli.py site flattened, zed-capture's logging-bootstrap init under a site-level noqa. A closing ruling collapsed the orchestrator's strategy vocabulary: `SpecProducer` is the one name (the command-wrapping callable class); the contract itself annotates `Callable[[Path], str]` inline, and the earlier alias/class pair is gone.

**Push state at final close (2026-09-22, evening) — everything is pushed.** placeframe's force-with-lease landed (branch in sync at the tip; the divergence was a docs-capstone commit folded after an earlier version of it had been pushed — resolved by replacement, hazard recorded in mechanics). bashrun's missed drift pin (`1830964`) is pushed, CI green, and **bashrun 0.1.1 is live on PyPI** (its publish job rides `ci.yml`, not a separate `release.yml`). Live on PyPI: python-devkit 0.1.3, bashrun 0.1.1, ci-devkit 0.1.2, unity-devkit 0.1.4, docker-devkit 0.1.3. **Operator ruling (2026-09-22): the six race-red CI runs (logger-conf, openapi-client-codegen, release-devkit, ci-devkit, docker-devkit, unity-devkit — their `==0.1.3` drift gates ran minutes before the 0.1.3 publish) will NOT be re-run** — they are transient, any next push re-runs CI against the live release, and the pending publishes (openapi-client-codegen **0.1.2**, logger-conf 0.1.2, release-devkit 0.1.3) ride those next green runs; the operator expects to change these repos again before placeframe's branch merges to `dev` anyway.

**Late close (2026-09-22 ~02:15Z): items 1 and 2 completed in-session.** The publisher bootstrap deadlock (frozen release-kit 0.1.0 / release-devkit 0.1.1 root-importing bashrun vs its emptied init — surfaced when openapi-client-codegen's re-run publish ImportError'd) was bridged by the shim: `--with bashrun==0.1.0` on every pinned-publisher invocation (openapi-client-codegen, bashrun, logger-conf via `ci.yml`; ci/docker/unity-devkit via `release.yml`), unblocked in dependency order with the operator pushing each fix (unity-devkit also needed its own lock floated to bashrun 0.1.2 for the alias re-export — its CI had been type-checking the repointed import against 0.1.0). Released tonight: openapi-client-codegen **0.1.2**, docker-devkit **0.1.4**, ci-devkit **0.1.3**, unity-devkit **0.1.5**, logger-conf **0.1.2** (plus python-devkit 0.1.3 / bashrun 0.1.2 earlier). placeframe floated everything (`ee018b66`, `66309d48`, pylocks regenerated), **full preflight green** on the final tree. The shim is **exited**: release-devkit 0.1.3 (the repointed publisher, self-publishing from its own checkout — no frozen-publisher problem) lands via a CI re-run, and every publish invocation repins to `release-devkit==0.1.3` with the `--with bashrun==0.1.0` guard deleted — openapi-client-codegen/bashrun/logger-conf's `ci.yml` (which also completes their deferred release-kit→release-devkit rename repin), the three devkits' `release.yml`, and placeframe's two workflows (whose `==0.1.1` pins were latently broken against bashrun 0.1.2 — `mirror-images` would have ImportError'd on the next CI run); the repin pushes self-verify by publishing their own patches through the new pin. The deterministic-enforcement discussion (API-diff semver gate, bounded ranges, hermetic publisher envs) is deferred by operator ruling 2026-09-22. release-devkit 0.1.3 itself remains unpublished (its CI still race-red, nothing pushed since — next push carries it; not blocking anything placeframe consumes).

**Observability reversal (2026-09-22, evening session):** the operator challenged the observability-images plan ("take nothing as gospel"); a registry probe settled it — `docker compose publish`'s OCI bundle is YAML-only, relative bind mounts in included files silently become empty directories (the failure that would have broken Make-it-Sing), and top-level `configs: content:` rides the bundle intact; the mirror digests are arm64 manifest lists pullable anonymously. Ruling: **stock consumption, wrappers deleted, observability-images cancelled before creation**. The staged implementation plan (two stages, two operator bench gates, cold-session brief) is [observability-stock-consumption.md](observability-stock-consumption.md); no code changed this session.

**Phase 5 (shared workflows) landed (2026-09-22, third session — every commit pending operator push; worked in a `prepo new shared-workflows` worktree to stay off the primary checkout, where a parallel agent session is executing Phase 4's stock consumption).** Items 13-15 complete; the landed-with records live in the [program doc](tooling-consolidation.md). Commit map: python-devkit `90141e9`+`f5343c7` (hosts `check.yml`, self-adopts via local path); release-devkit `098b8e9`/`fd84825`/`f2c2afa` (publish composite action; own ci collapsed; own `release.yml` stays in-process per its self-publication invariant); unity-devkit `85726f3`/`f20f592`/`cbb49c2` (hosts `unity-build.yml` verbatim; ci + release collapsed; docs); the five leaves one code + one prose commit each (bashrun `565bc81`/`285ebdb`, logger-conf `5d0978a`/`6f850fc`, openapi-client-codegen `c9cb895`/`440df8f`, ci-devkit `7515eea`/`e6ce7e7`, docker-devkit `eeb14dd`/`28353b6`); placeframe `ce0b8f37`/`d71da3b5`/`d16a7991` on `shared-workflows`, since merged into `tooling-consolidation`; Make-it-Sing `feature/unity-ci` `d8b69cc` (workflow repin + dep repin — the branch was already diverged ↑10↓10 before the change and still carries the TEMPORARY air-gated build-env commit `e49afb7`; the operator decides rebase-vs-merge at push time). **Push order, leaves-first: python-devkit first** (every collapsed check job pins its SHA), **then release-devkit** (its green CI mints 0.1.3 via in-process self-publication — the action's internal `==0.1.3` pin resolves only after), then unity-devkit, the five leaves, placeframe `tooling-consolidation`, and Make-it-Sing in any order. Known follow-up recorded in item 13: `github.head_ref` inline interpolation in unity-build.yml's build step (actionlint).

**Next session pickup:** Phase 5 is committed and awaiting the operator push wave (above); Phase 4 (observability stock consumption) is in flight in the parallel agent session — the staged plan, operator gates 1 and 2, and the probe evidence live in [observability-stock-consumption.md](observability-stock-consumption.md); Phase 6 (un-park thread 4) opens once both land on `tooling-consolidation`. Standing operator queue: the three old-name terminal deprecation releases (stack-toolkit, unity-buildkit, release-kit); the layout-lint verb proposal for the `__all__` ban (awaiting go); the deferred deterministic-enforcement layers; rubidium-build's `master` carries no upstream; pulsar ahead-10. The working tree's two CaptureTool `.asset.meta` files remain uncommitted by design — keep them out of every commit.

Session mechanics worth reusing (2026-09-21 sessions): after a mount rename, repo venvs are dead (entry-point shebangs hardcode the old path) — `rm -rf .venv && uv sync` recreates; the PyPI JSON endpoint is CDN-lagged after a publish — verify via the Release run log (`gh run view <id> --log`, grep `Tagged:`), not the registry; git cannot split an unborn branch's root commit (restore --staged fails against unborn HEAD) — a bootstrap repo lands scaffold+docs as one root commit; consumer repos whose deps aren't yet on PyPI commit their flips with the lock regen deferred to a post-publish wave (leaves-first), verified by the next session rather than assumed; and a consumer can be verified against an unreleased devkit without touching the committed lock — `uv pip install --no-deps <local repo>` into the venv, invoke `.venv/bin/<entry-point>` directly, then `uv sync` to restore lock state. From the RUF067 wave: an unreleased devkit's verbs run cross-repo via `uvx --from /workspace/<devkit> <verb>`; rebases over the protected asset dirt need `--autostash`; **re-verify branch-locality immediately before every fixup/autosquash when the operator pushes from the host concurrently** — the shared `.git` means origin can advance mid-session, and folding into an already-pushed commit forks the branch (this session's one divergence: the docs tip, force-with-lease to resolve); type-checking a consumer against an unreleased library requires the local install too (basedpyright reads the venv, not the worktree); and PLC0414 fires on pyright's same-name-alias re-export idiom outside `__init__.py` — the sanctioned home for that suppression is the wrapper module's file-level noqa header (bash.py's, extended for `CalledProcessError`). From the ruff-canonicalization session: B008 exempts immutable-annotated parameters (`bool = typer.Option(False, ...)` passes; `Path`, enums, `list | None` fail) and the Annotated conversion must keep the `= default` tail and order required parameters first — a bare Annotated parameter after an old-idiom defaulted one is a syntax error, so signatures convert whole; ruff ≥0.15's preview RUF201 polices rule names in the discovered config's selectors only (extended bases are exempt) — layer entries use rule names, the canonical keeps placeframe's codes; prepo has no CI at all (adoption ungated); the three unrenamed repos' `ci.yml` publish jobs still pin `release-kit==0.1.0` (thread-3-era shape, functional against the frozen old-name PyPI deps — Phase 5 territory, untouched); never append to a workflow YAML with `>>` (it lands after the final job's last step) — edit by anchor.

Dev-channel runbook (operator): registry setup is done — PyPI pending publishers bound to the `pypi-placeframe-common` / `pypi-placeframe-core` environments (environment names belong only in the GitHub environment and the publisher's environment field; the PyPI project names are `placeframe-common` / `placeframe-core`), npm trusted publishers on all six `org.outernet.*` packages (`placeframe-release.yml`, filename only, no environment, direct `npm publish` allowed, token path locked to disallow-bypass-2FA). To publish dev prereleases after a green dev CI run: `gh workflow run placeframe-release.yml --ref dev -f dev-publish=true -f dev-run-id=<green CI run id>`; retries with the same run id are idempotent on every feed (nuget `--skip-duplicate`, npm EPUBLISHCONFLICT catch, PyPI `--check-url`). The publish jobs now invoke release-kit uvx-isolated (`uvx --from release-kit==0.1.0 …`) — upgrading release-kit in placeframe is a one-line workflow edit.

Sandbox verification note: the toolchain uv (0.11.14) predates the repo's uv pin (0.12.15), and `uv run` strips its own bin dir from child PATH while the sandbox's `UV_PYTHON` points nested `uv pip` at the externally-managed base interpreter — codegen and preflight therefore fail under plain `uv run`. Workaround: `~/.local/bin/uv` is already 0.12.15 (if missing, `uv self update` installs it); invoke venv entry points directly with `env -u UV_PYTHON PATH="$HOME/.local/bin:$PATH" .venv/bin/preflight` (same pattern for `uv sync`, `lock-python`, `pytest`, `publish-dev --dry-run`). Always capture exit codes explicitly (`... ; echo EXIT=$?`) rather than piping through `tail`, which masks failures. The working tree carries pre-existing unrelated dirt (two CaptureTool `.asset.meta` files) — keep it out of all commits; typer list options take one value per flag (`--exclude a --exclude b`), never space-separated.

Linear is nonfunctional in this environment — ignore every Linear rule and workflow (including trackable-work conventions and the design-doc orphan lint); this resume protocol replaces them for this initiative.
